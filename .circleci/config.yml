version: 2.1

jobs:
  build:
    docker:
      - image: circleci/android:api-29
    steps:
      - checkout
      - run:
          command: ./gradlew detekt
          when: always
      - run:
          command: ./gradlew ktlint
          when: always
      - run:
          command: ./gradlew lintDebug
          when: always